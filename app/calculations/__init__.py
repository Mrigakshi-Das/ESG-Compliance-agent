"""Deterministic calculation functions, one module per domain (emissions.py,
energy.py, water.py, waste.py) plus scoring.py for the cross-domain math
(variance-vs-target, priority scoring, overall readiness score). Pure
Python, unit-tested, never delegated to the LLM. Implemented in Phase 3."""
