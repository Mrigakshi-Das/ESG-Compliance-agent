"""Deterministic water calculations (post-MVP metric, stubbed from Phase 1)."""


def calculate_water_intensity(water_consumption_m3: float, production_tonnes: float) -> float:
    """Return water consumed (m3) per tonne of production."""
    raise NotImplementedError("Implemented in a later phase (post-MVP metric)")
